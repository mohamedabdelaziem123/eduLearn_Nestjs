export enum GenderEnum {
  male = 'male',
  female = 'female',
}

export enum RoleEnum {
  student = 'student',
  teacher = 'teacher',
  admin = 'admin',
}

export enum providerEnum {
  google = 'google',
  system = 'system',
}
